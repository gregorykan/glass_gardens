<%= semantic_form_for @garden do |f| %>
  <%= f.inputs do %>
    <%= f.input :name %>
    <%= f.input :authors, :as => :select %>
  <% end %>
  <%= f.actions do %>
    <%= f.action :submit %>
  <% end %>
<% end %>

<%= link_to "back to list of gardens", gardens_path %>